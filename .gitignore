# Data (may contain PHI: MRNs, labs, the source workbook) — never commit
data/
patches/
*.nii
*.nii.gz
*.dcm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Secrets / local config (credentials must never be committed)
.env
.env.*
!.env.example

# Misc
*.log
.DS_Store

# Archived pre-extraction Dr Stone material moved out of the PRISM repo (PHI + stale)
_prism_origin/
